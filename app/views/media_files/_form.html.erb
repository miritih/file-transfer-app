<%= form_with(model: media_file, :multipart => true) do |form| %>
  <% if media_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(media_file.errors.count, "error") %> prohibited this media_file from being saved:</h2>
      <ul>
      <% media_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
    <% end %>
    <div class="jumbotron">
      <div class="form-group row">
        <label for="files" class="col-sm-2 col-form-label">Files</label>
        <div class="col-sm-10">
          <%= form.file_field :files, required: true, multiple: true, direct_upload: true, class: 'form-control' %>
        </div>
      </div>
      <div class="form-group row">
        <div class="col-sm-12">
          <button type="submit" class="btn btn-primary float-right">Upload Files</button>
        </div>
      </div>
  <% end %>
</div>
